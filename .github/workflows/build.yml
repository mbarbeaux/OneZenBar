name: Build

# pull_request only (no push trigger on feature branches): a push to a branch with an open PR
# would otherwise run the gate twice — and the PR run is the valuable one, since it validates
# the MERGE result against the target branch. Open a PR (draft is fine) to get CI on a branch.
on:
  pull_request:
    branches:
      - develop
      - main

# Cancel superseded runs on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/quality-gate.yml
    permissions:
      contents: read
    # Like env, secrets do NOT propagate to reusable workflows — required for the MSIX signing
    # secrets (SIGNING_CERTIFICATE_*) and the GH_PACKAGES_PAT restore credential used inside the gate
    secrets: inherit
